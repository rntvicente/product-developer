const { create } = require("./create-levels-controller");
const { getAll } = require("./get-all-levels-controller");
const { findById } = require("./find-by-id-levels-constroller");

module.exports = { create, getAll, findById };
